import React from 'react'

interface routercard {
    title:string;
    image:string;
    link:string;
}
const Gridinfocard: React.FC<routercard> = ({title,image,link}) => {
  return (
    <div>
        
    </div>
  )
}

export default Gridinfocard